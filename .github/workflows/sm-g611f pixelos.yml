name: PixelOS Android 15 (Pure ARM64 Conversion) for SM-G611F

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    
    steps:
    - name: Depoyu Al (Checkout)
      uses: actions/checkout@v4

    - name: Geliştirici Paketlerini Kur
      run: |
        sudo apt-get update
        sudo apt-get install -y bc bison build-essential ccache curl flex g++-multilib git gnupg gperf imagemagick lib32readline-dev lib32z1-dev libelf-dev liblz4-tool libsdl1.2-dev libssl-dev libxml2 libxml2-utils lzop pngcrush rsync schedtool squashfs-tools xsltproc zip zlib1g-dev python3 python-is-python3

    - name: Manuel Disk Alanı Temizliği Yap
      run: |
        sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /usr/share/swift /usr/local/share/boost "$AGENT_TOOLSDIRECTORY"
        sudo docker image prune --all --force
        df -h

    - name: Repo Aracını Kur
      run: |
        mkdir -p ~/bin
        curl https://storage.googleapis.com/git-repo-downloads/repo > ~/bin/repo
        chmod a+x ~/bin/repo
        sudo ln -sf ~/bin/repo /usr/bin/repo

    - name: Git Kimlik Bilgilerini Ayarla
      run: |
        git config --global user.name "Geliştirici"
        git config --global user.email "developer@example.com"

    # İlk seferde kodları indirecek, sonraki çalıştırmalarda 20 dakika bekletmeyecek önbellek adımı
    - name: Repo Önbelleğini Kontrol Et / Yükle
      uses: actions/cache@v4
      with:
        path: android/.repo
        key: repo-pixelos-fifteen-${{ hashFiles('.github/workflows/*.yml') }}
        restore-keys: |
          repo-pixelos-fifteen-

    - name: PixelOS Android 15 Kaynak Kodunu İndir
      run: |
        mkdir -p android && cd android
        repo init -u https://github.com/PixelOS-AOSP/manifest.git -b fifteen --git-lfs
        repo sync -c -j$(nproc --all) --force-sync --no-clone-bundle --no-tags

    # --- SENİN VERDİĞİN GERÇEK CİHAZ AĞACI BURAYA EKLENDİ ---
    - name: Cihaz Ağaçlarını Çek
      run: |
        cd android
        # Senin gönderdiğin gerçek SM-G611F cihaz ağacı linki
        git clone https://github.com/AndroidBlobs/device_samsung_on7xreflte.git device/samsung/g611f
        
        # Exynos 7870 topluluğunun ortak vendor ve kernel ağaçları
        git clone https://github.com/Exynos7870/android_vendor_samsung_on7xelte.git vendor/samsung/g611f
        git clone https://github.com/Exynos7870/android_kernel_samsung_exynos7870.git kernel/samsung/exynos7870

    - name: Cihaz Ağacını 64-Bit Mimariye Zorla (ARM64 Conversion)
      run: |
        cd android/device/samsung/g611f
        sed -i '/TARGET_ARCH/d' BoardConfig.mk
        sed -i '/TARGET_CPU/d' BoardConfig.mk
        cat <<EOF >> BoardConfig.mk

        # --- YAML Tarafından Otomatik Eklenen 64-Bit Yapılandırması ---
        TARGET_ARCH := arm64
        TARGET_ARCH_VARIANT := armv8-a
        TARGET_CPU_ABI := arm64-v8a
        TARGET_CPU_VARIANT := generic

        TARGET_2ND_ARCH := arm
        TARGET_2ND_ARCH_VARIANT := armv7-a-neon
        TARGET_2ND_CPU_ABI := armeabi-v7a
        TARGET_2ND_CPU_ABI2 := armeabi
        TARGET_2ND_CPU_VARIANT := generic

        TARGET_USES_64_BIT_BINDER := true
        # -------------------------------------------------------------
        EOF
        echo "BoardConfig.mk 64-bit moduna uyarlandı."

    - name: Kernel Derleyicisini ARM64 Olarak Ayarla
      run: |
        cd android
        export TARGET_KERNEL_ARCH=arm64
        export ARCH=arm64

    - name: CCACHE Önbelleğini Yapılandır
      run: |
        export USE_CCACHE=1
        export CCACHE_EXEC=/usr/bin/ccache
        ccache -M 50G

    - name: ARM64 Derlemeyi Başlat
      run: |
        cd android
        source build/envsetup.sh
        
        # Ağacın içindeki isimlendirmeye göre lunch hedefini seçiyoruz. 
        # Cihaz klasörü 'g611f' olduğu için 'pixelos_g611f-userdebug' veya 'aosp_g611f-userdebug' deneyecek.
        lunch pixelos_g611f-userdebug || lunch aosp_g611f-userdebug || lunch pixelos_on7xreflte-userdebug
        
        mka bacon -j$(nproc --all)

    - name: Üretilen ARM64 ROM Zip Dosyasını GitHub'a Yükle
      uses: actions/upload-artifact@v4
      with:
        name: PixelOS-Android15-64Bit-SM-G611F
        path: android/out/target/product/g611f/*.zip
        
