name: macOS Tailscale Simple

on:
  workflow_dispatch:

jobs:
  run-macos:
    runs-on: macos-latest
    timeout-minutes: 360

    steps:
      - name: Kullanıcı Adı ve Şifreyi Ayarla ve VNC'yi Etkinleştir
        run: |
          sudo dscl . -create /Users/siksiken
          sudo dscl . -passwd /Users/siksiken siksiken
          sudo dscl . -append /Group/admin GroupMembership siksiken
          sudo launchctl load -w /System/Library/LaunchDaemons/com.apple.screensharing.plist
          sudo defaults write /Library/Preferences/com.apple.AppleVNCServer VNCAuthentication -int 1
          sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart -activate -configure -access -on -users siksiken -privs -all -restart -agent -menu

      - name: Tailscale CLI Kurulumu ve Bağlantı
        run: |
          brew install tailscale
          sudo tailscaled &
          sleep 3
          sudo tailscale up --authkey="${{ secrets.TAILSCALE_AUTHKEY }}" --accept-routes

      - name: Oturumu Açık Tutma
        run: |
          echo "macOS runner aktif."
          while true; do
              tailscale status > /dev/null
              sleep 20
          done
          
