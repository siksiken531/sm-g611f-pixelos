name: Ubuntu RDP XFCE

on:
  workflow_dispatch:

jobs:
  run-ubuntu:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - name: XFCE ve XRDP Kurulumu
        run: |
          sudo apt-get update -y
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends xfce4 xfce4-goodies xrdp
          sudo systemctl enable xrdp
          
          # Kullanıcı oluşturma ve yetkilendirme
          sudo useradd -m -s /bin/bash siksiken || true
          echo "siksiken:siksiken" | sudo chpasswd
          sudo usermod -aG sudo siksiken
          
          # XRDP oturum yapılandırması
          echo "startxfce4" | sudo tee /home/siksiken/.xsession > /dev/null
          sudo chown siksiken:siksiken /home/siksiken/.xsession
          sudo chmod +x /home/siksiken/.xsession
          
          sudo systemctl restart xrdp

      - name: Tailscale Kurulumu ve Bağlantı
        run: |
          curl -fsSL https://tailscale.com/install.sh | sh
          sudo tailscale up --authkey="${{ secrets.TAILSCALE_AUTHKEY }}" --accept-routes
          
          tsIp=$(tailscale ip -4)
          echo "=================================================="
          echo "TAILSCALE IP ADRESİNİZ (RDP için): $tsIp"
          echo "=================================================="

      - name: Oturumu Açık Tutma
        run: |
          echo "Ubuntu RDP runner aktif."
          while true; do
              tailscale status > /dev/null
              sleep 20
          done
          
