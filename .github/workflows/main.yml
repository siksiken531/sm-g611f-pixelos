name: Sanal makineler çalıştırma

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - name: SSH (Tmate) Başlat
        uses: mxschmitt/action-tmate@v3
        with:
          install-dependencies: true

      - name: Oturumu Açık Tut
        run: sleep 21600
        
